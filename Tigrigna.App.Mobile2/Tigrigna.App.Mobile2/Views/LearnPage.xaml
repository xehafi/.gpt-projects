<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Tigrigna.App.Mobile2.Views.LearnPage"
    x:Name="ThisPage"
    Title="Learn">

    <VerticalStackLayout Padding="16" Spacing="12">
        <Label Text="{Binding LearnTitle}" FontSize="24" FontAttributes="Bold" />
        <Label Text="{Binding StartHereTitle}" FontSize="18" />

        <CollectionView ItemsSource="{Binding Skills}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="0,6" HasShadow="True">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <Label Grid.Column="0" Text="{Binding Icon}" FontSize="28" />
                            <VerticalStackLayout Grid.Column="1" Spacing="2">
                                <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Subtitle}" FontSize="13" />
                            </VerticalStackLayout>
                            <Button Grid.Column="2"
                                    Text="Start"
                                    Command="{Binding BindingContext.OpenLessonCommand, Source={x:Reference ThisPage}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
